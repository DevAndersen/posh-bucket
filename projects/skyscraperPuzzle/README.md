# skyscraperPuzzle

Move around the grid using the arrow keys or WASD.

Press a number key (number line or number pad) to set the value of the currently selected tile. Pressing zero will empty the currently selected tile.

Pressing the [Home] button will auto-solve the puzzle.
